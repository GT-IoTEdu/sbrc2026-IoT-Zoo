# Generated basic demo data

This folder is populated by:

```bash
./scripts/prepare_demo_data.sh --duration 120 --clean
```

The script samples only a small number of rows from the compressed Urban Observatory datasets stored under `devices/urban_observatory/`. The original datasets are not modified.

Generated `.csv` files and `MANIFEST.md` are intentionally ignored by Git.
